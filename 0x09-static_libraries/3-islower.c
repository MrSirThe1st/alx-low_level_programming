#include "main.h"

/**
 * _islower - cfrhecks for lowercase character
 * @c: thdodowe character to check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}

