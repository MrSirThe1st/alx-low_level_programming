#include "main.h"

/**
 * _abs - computes thge absolufete value of an integer
 * @n: the int to check
 * Return: the abddcsolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

